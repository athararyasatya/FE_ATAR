import 'package:flutter/material.dart';

class CashierTransaction extends StatelessWidget {
  const CashierTransaction({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Cashier Transaction')),
      body: const Center(
        child: Text('Cashier Transaction - Coming Soon'),
      ),
    );
  }
}